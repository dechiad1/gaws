// +build ignore

package util

import (
	//"fmt"
	//"os"
)

type DisplayUnit struct {
	//var header []string
	//row := make([]string, 5)
}

//fmt.Fprintf(w, "\n %s\t%s\t%s\t%s", "Name (from tag)", "Private IP", "Security Groups", "Public DNS")
func (DisplayUnit du) SetHeader([]string input) {
	//header = input
}

func (DisplayUnit du) Print() {
	//header
	//for _, string := range du.header {

	//}
	//fmt.Println()
}



